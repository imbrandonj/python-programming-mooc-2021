# A square of hashes

def hash_square(length):
    """Prints out a square of hash characters
    the argument specifies the length and width"""

    for i in range(length):
        print("#" * length)

hash_square(9)
